import React from 'react'
import './Search.css';

const Search = () => (
    <div>
        <form>
            <p>Where:</p><input/>
            <p>From:</p><input/>
        </form>
        <button>Search</button>
    </div>
);

export default Search