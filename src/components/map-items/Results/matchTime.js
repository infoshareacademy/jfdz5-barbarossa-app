export const matchTime = (foundLines, time) => {

    const inputTime = time.hour * 3600 + time.minutes * 60 + time.seconds

    // foundLines.map(
    //     foundLines => ({
    //
    //     })
    // )

    return inputTime

}

