import React from 'react'

import {Col} from 'react-bootstrap'

const Stops = () => (
    <Col md={6}>
        <div className="main-panel">
            <h1>Stops</h1>
        </div>
    </Col>
);

export default Stops