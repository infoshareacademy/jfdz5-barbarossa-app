// Action types

// Action creators

// Initial state
const initialState = {
    departureStop: null,
    arrivalStop: null,
}

// Reducer
export default (state = initialState, action) => {
    switch(action.type) {
        default:
            return state
    }
}